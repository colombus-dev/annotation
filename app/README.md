# Annotation Platform

A React + Vite frontend for annotating source code.

## Setup

```bash
npm install
npm run dev
```

The dev server runs on `http://localhost:5174` and proxies API requests to `http://localhost:8000`.

## Features

- Upload `.py` and `.ipynb` files
- View source code with line numbers
- Annotate code lines
